<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tableau de bord - <%= user ? user.name : 'Utilisateur' %></title>

    <!-- Bootstrap 5 CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        font-family: "Inter", sans-serif;
      }

      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        margin: 0;
        padding: 0;
      }

      .dashboard-container {
        min-height: 100vh;
        background: rgba(255, 255, 255, 0.1);
      }

      .navbar-custom {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
      }

      .navbar-brand {
        font-weight: 700;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .main-content {
        padding: 2rem;
      }

      .welcome-card {
        background: white;
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        padding: 2rem;
        margin-bottom: 2rem;
        animation: fadeInUp 0.6s ease-out;
      }

      .stats-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        padding: 1.5rem;
        text-align: center;
        transition: all 0.3s ease;
        height: 100%;
      }

      .stats-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      }

      .stats-card .icon {
        width: 60px;
        height: 60px;
        border-radius: 15px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem;
        font-size: 1.5rem;
        color: white;
      }

      .stats-card.income .icon {
        background: linear-gradient(135deg, #28a745, #20c997);
      }

      .stats-card.expense .icon {
        background: linear-gradient(135deg, #dc3545, #fd7e14);
      }

      .stats-card.budget .icon {
        background: linear-gradient(135deg, #007bff, #6610f2);
      }

      .stats-card.savings .icon {
        background: linear-gradient(135deg, #ffc107, #fd7e14);
      }

      .stats-card h3 {
        font-size: 2rem;
        font-weight: 700;
        margin: 0.5rem 0;
        color: #333;
      }

      .stats-card p {
        color: #666;
        margin: 0;
        font-weight: 500;
      }

      .quick-actions {
        background: white;
        border-radius: 20px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        padding: 2rem;
        margin-top: 2rem;
      }

      .btn-action {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        border-radius: 12px;
        color: white;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
        margin: 0.5rem;
      }

      .btn-action:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        color: white;
      }

      .btn-secondary-action {
        background: transparent;
        border: 2px solid #667eea;
        border-radius: 12px;
        color: #667eea;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
        margin: 0.5rem;
      }

      .btn-secondary-action:hover {
        background: #667eea;
        color: white;
      }

      .dropdown-menu {
        border-radius: 12px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        border: none;
      }

      .dropdown-item {
        padding: 0.75rem 1.25rem;
        transition: all 0.3s ease;
      }

      .dropdown-item:hover {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @media (max-width: 768px) {
        .main-content {
          padding: 1rem;
        }

        .welcome-card,
        .quick-actions {
          padding: 1.5rem;
        }

        .stats-card h3 {
          font-size: 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="dashboard-container">
      <!-- Navigation -->
      <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
          <a class="navbar-brand" href="/dashboard">
            <i class="fas fa-wallet me-2"></i>
            Gestion de Budget
          </a>

          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <a class="nav-link active" href="/dashboard">
                  <i class="fas fa-home me-1"></i>
                  Tableau de bord
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/transactions">
                  <i class="fas fa-exchange-alt me-1"></i>
                  Transactions
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/budgets">
                  <i class="fas fa-chart-pie me-1"></i>
                  Budgets
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/savings">
                  <i class="fas fa-piggy-bank me-1"></i>
                  Épargne
                </a>
              </li>
            </ul>

            <div class="dropdown">
              <a
                class="nav-link dropdown-toggle d-flex align-items-center"
                href="#"
                role="button"
                data-bs-toggle="dropdown"
              >
                <i class="fas fa-user-circle me-2 fs-4"></i>
                <%= user ? user.name : 'Utilisateur' %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li>
                  <a class="dropdown-item" href="/profile">
                    <i class="fas fa-user me-2"></i>
                    Mon Profil
                  </a>
                </li>
                <li>
                  <a class="dropdown-item" href="/settings">
                    <i class="fas fa-cog me-2"></i>
                    Paramètres
                  </a>
                </li>
                <li><hr class="dropdown-divider" /></li>
                <li>
                  <a class="dropdown-item text-danger" href="/auth/logout">
                    <i class="fas fa-sign-out-alt me-2"></i>
                    Déconnexion
                  </a>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </nav>

      <!-- Main Content -->
      <div class="main-content">
        <div class="container">
          <!-- Welcome Card -->
          <div class="welcome-card">
            <div class="row align-items-center">
              <div class="col-md-8">
                <h1 class="mb-3">
                  <i class="fas fa-sun text-warning me-2"></i>
                  Bonjour, <%= user ? user.name : 'Utilisateur' %>!
                </h1>
                <p class="lead mb-0">
                  Bienvenue sur votre tableau de bord de gestion financière.
                  Suivez vos revenus, dépenses et objectifs d'épargne en temps
                  réel.
                </p>
              </div>
              <div class="col-md-4 text-center">
                <i
                  class="fas fa-chart-line"
                  style="font-size: 4rem; color: #667eea"
                ></i>
              </div>
            </div>
          </div>

          <!-- Statistics Cards -->
          <div class="row g-4 mb-4">
            <div class="col-md-3 col-sm-6">
              <div class="stats-card income">
                <div class="icon">
                  <i class="fas fa-arrow-up"></i>
                </div>
                <h3>€0.00</h3>
                <p>Revenus ce mois</p>
              </div>
            </div>
            <div class="col-md-3 col-sm-6">
              <div class="stats-card expense">
                <div class="icon">
                  <i class="fas fa-arrow-down"></i>
                </div>
                <h3>€0.00</h3>
                <p>Dépenses ce mois</p>
              </div>
            </div>
            <div class="col-md-3 col-sm-6">
              <div class="stats-card budget">
                <div class="icon">
                  <i class="fas fa-chart-pie"></i>
                </div>
                <h3>0</h3>
                <p>Budgets actifs</p>
              </div>
            </div>
            <div class="col-md-3 col-sm-6">
              <div class="stats-card savings">
                <div class="icon">
                  <i class="fas fa-piggy-bank"></i>
                </div>
                <h3>€0.00</h3>
                <p>Objectifs d'épargne</p>
              </div>
            </div>
          </div>

          <!-- Quick Actions -->
          <div class="quick-actions">
            <h3 class="mb-4">
              <i class="fas fa-bolt text-primary me-2"></i>
              Actions rapides
            </h3>
            <div class="text-center">
              <br />

              <a
                href="/transactions/create"
                class="btn btn-primary btn-lg me-2"
                id="transaction-btn"
              >
                <i class="fas fa-plus me-2"></i>
                Ajouter une transaction
              </a>
              <a
                href="/transactions"
                class="btn btn-success btn-lg me-2"
                title="Gérer vos transactions"
              >
                <i class="fas fa-list me-2"></i>
                Mes Transactions
              </a>
              <button
                type="button"
                class="btn btn-outline-primary btn-lg"
                data-bs-toggle="modal"
                data-bs-target="#createBudgetModal"
              >
                <i class="fas fa-chart-pie me-2"></i>
                Créer un budget
              </button>
              <a href="/budgets" class="btn btn-outline-warning btn-lg">
                <i class="fas fa-wallet me-2"></i>
                Gérer mes budgets
              </a>
              <button
                type="button"
                class="btn btn-success btn-lg"
                data-bs-toggle="modal"
                data-bs-target="#createSavingsGoalModal"
              >
                <i class="fas fa-piggy-bank me-2"></i>
                Nouvel objectif d'épargne
              </button>
              <a href="/reports" class="btn btn-outline-info btn-lg">
                <i class="fas fa-chart-bar me-2"></i>
                Voir les rapports
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Modal Créer un Budget -->
    <div
      class="modal fade"
      id="createBudgetModal"
      tabindex="-1"
      aria-labelledby="createBudgetModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-lg">
        <div class="modal-content">
          <div class="modal-header bg-primary text-white">
            <h5 class="modal-title" id="createBudgetModalLabel">
              <i class="fas fa-wallet me-2"></i>
              Créer un nouveau budget
            </h5>
            <button
              type="button"
              class="btn-close btn-close-white"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <form id="createBudgetForm" method="POST" action="/budgets">
              <div class="row">
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="budgetCategory" class="form-label">
                      <i class="fas fa-list me-1"></i>
                      Catégorie *
                    </label>
                    <select
                      class="form-select"
                      id="budgetCategory"
                      name="category"
                      required
                    >
                      <option value="">Sélectionner une catégorie</option>
                      <option value="alimentation">🍽️ Alimentation</option>
                      <option value="transport">🚗 Transport</option>
                      <option value="logement">🏠 Logement</option>
                      <option value="loisirs">🎮 Loisirs</option>
                      <option value="sante">🏥 Santé</option>
                      <option value="education">📚 Éducation</option>
                      <option value="vetements">👕 Vêtements</option>
                      <option value="autres">📦 Autres</option>
                    </select>
                  </div>
                </div>
              </div>

              <div class="row">
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="budgetLimitAmount" class="form-label">
                      <i class="fas fa-euro-sign me-1"></i>
                      Montant limite *
                    </label>
                    <div class="input-group">
                      <span class="input-group-text">€</span>
                      <input
                        type="number"
                        class="form-control"
                        id="budgetLimitAmount"
                        name="limitAmount"
                        step="0.01"
                        min="0"
                        required
                        placeholder="0.00"
                      />
                    </div>
                  </div>
                </div>
              </div>

              <div class="row">
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="budgetMonth" class="form-label">
                      <i class="fas fa-calendar me-1"></i>
                      Mois *
                    </label>
                    <select
                      class="form-select"
                      id="budgetMonth"
                      name="month"
                      required
                    >
                      <option value="">Sélectionner un mois</option>
                      <option value="1">Janvier</option>
                      <option value="2">Février</option>
                      <option value="3">Mars</option>
                      <option value="4">Avril</option>
                      <option value="5">Mai</option>
                      <option value="6">Juin</option>
                      <option value="7">Juillet</option>
                      <option value="8">Août</option>
                      <option value="9">Septembre</option>
                      <option value="10">Octobre</option>
                      <option value="11">Novembre</option>
                      <option value="12">Décembre</option>
                    </select>
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="budgetYear" class="form-label">
                      <i class="fas fa-calendar-alt me-1"></i>
                      Année *
                    </label>
                    <select
                      class="form-select"
                      id="budgetYear"
                      name="year"
                      required
                    >
                      <option value="">Sélectionner une année</option>
                      <option value="2024">2024</option>
                      <option value="2025">2025</option>
                      <option value="2026">2026</option>
                      <option value="2027">2027</option>
                    </select>
                  </div>
                </div>
              </div>
            </form>
          </div>
          <div class="modal-footer">
            <button
              type="button"
              class="btn btn-secondary"
              data-bs-dismiss="modal"
            >
              <i class="fas fa-times me-1"></i>
              Annuler
            </button>
            <button
              type="submit"
              form="createBudgetForm"
              class="btn btn-primary"
            >
              <i class="fas fa-save me-1"></i>
              Créer le budget
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
      // Add some interactivity
      document.addEventListener("DOMContentLoaded", function () {
        // Animate stats cards on load
        const statsCards = document.querySelectorAll(".stats-card");
        statsCards.forEach((card, index) => {
          setTimeout(() => {
            card.style.animation = `fadeInUp 0.6s ease-out ${
              index * 0.1
            }s both`;
          }, 100);
        });
      });

      // Debug complet du bouton transaction
      const transactionLink = document.querySelector(
        'a[href="/transactions/create"]'
      );
      console.log("🔍 Bouton trouvé:", transactionLink);

      if (transactionLink) {
        console.log("✅ Bouton existe");
        console.log("📍 Position:", transactionLink.getBoundingClientRect());
        console.log("👆 Styles:", window.getComputedStyle(transactionLink));

        // Test de clic manuel
        transactionLink.addEventListener("click", function (e) {
          console.log("🎯 CLIC DÉTECTÉ!");
          console.log("🔗 URL:", this.href);
          console.log("⚡ Event:", e);

          // Test si on peut naviguer
          try {
            console.log("🚀 Tentative de navigation...");
            window.location.href = this.href;
          } catch (error) {
            console.error("❌ Erreur de navigation:", error);
          }
        });
      } else {
        console.error("❌ Bouton NON TROUVÉ!");
      }

      // Test général
      console.log("✅ Dashboard chargé à", new Date().toLocaleTimeString());

      // Gestion du modal de création de budget
      const createBudgetModal = document.getElementById("createBudgetModal");
      const createBudgetForm = document.getElementById("createBudgetForm");

      if (createBudgetModal && createBudgetForm) {
        console.log("✅ Modal et formulaire trouvés");

        // Soumission du formulaire
        createBudgetForm.addEventListener("submit", function (e) {
          e.preventDefault();
          console.log("📝 Formulaire soumis");

          // Récupérer les valeurs
          const budgetCategory = document.getElementById("budgetCategory");
          const budgetLimitAmount =
            document.getElementById("budgetLimitAmount");
          const budgetMonth = document.getElementById("budgetMonth");
          const budgetYear = document.getElementById("budgetYear");

          if (
            !budgetCategory ||
            !budgetLimitAmount ||
            !budgetMonth ||
            !budgetYear
          ) {
            console.error("❌ Éléments du formulaire non trouvés");
            return;
          }

          const category = budgetCategory.value.trim();
          const limitAmount = parseFloat(budgetLimitAmount.value);
          const month = parseInt(budgetMonth.value);
          const year = parseInt(budgetYear.value);

          console.log("📋 Données du formulaire:", {
            category,
            limitAmount,
            month,
            year,
          });

          // Validation
          if (!category || category === "") {
            console.log("❌ Catégorie invalide");
            budgetCategory.classList.add("is-invalid");
            budgetCategory.focus();
            return;
          }

          if (!limitAmount || limitAmount <= 0) {
            console.log("❌ Montant limite invalide");
            budgetLimitAmount.classList.add("is-invalid");
            budgetLimitAmount.focus();
            return;
          }

          if (!month || month < 1 || month > 12) {
            console.log("❌ Mois invalide");
            budgetMonth.classList.add("is-invalid");
            budgetMonth.focus();
            return;
          }

          if (!year || year < 2024) {
            console.log("❌ Année invalide");
            budgetYear.classList.add("is-invalid");
            budgetYear.focus();
            return;
          }

          // Bouton de soumission (dans le modal footer)
          const submitBtn = document.querySelector(
            'button[form="createBudgetForm"]'
          );
          if (!submitBtn) {
            console.error("❌ Bouton de soumission non trouvé");
            return;
          }
          console.log("✅ Bouton de soumission trouvé:", submitBtn);

          const originalText = submitBtn.textContent;

          // Animation de chargement
          submitBtn.textContent = "Création...";
          submitBtn.disabled = true;

          // Préparer les données
          const budgetData = {
            category: category,
            limitAmount: limitAmount,
            month: month,
            year: year,
          };

          console.log("📤 Envoi vers API:", budgetData);

          // Envoyer à l'API
          fetch("/budgets", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify(budgetData),
          })
            .then(function (response) {
              console.log("📡 Réponse reçue:", response.status);
              return response.json();
            })
            .then(function (data) {
              console.log("📥 Données de réponse:", data);

              if (data.success) {
                // Succès
                submitBtn.textContent = "Budget créé !";
                submitBtn.classList.remove("btn-primary");
                submitBtn.classList.add("btn-success");

                alert("✅ Budget créé avec succès !");

                // Fermer le modal et recharger
                setTimeout(function () {
                  const modal = bootstrap.Modal.getInstance(createBudgetModal);
                  if (modal) {
                    modal.hide();
                  }
                  window.location.reload();
                }, 1500);
              } else {
                // Erreur
                submitBtn.textContent = "Erreur";
                submitBtn.classList.remove("btn-primary");
                submitBtn.classList.add("btn-danger");

                alert("❌ Erreur: " + (data.message || "Erreur inconnue"));

                // Réinitialiser le bouton
                setTimeout(function () {
                  submitBtn.textContent = originalText;
                  submitBtn.disabled = false;
                  submitBtn.classList.remove("btn-danger");
                  submitBtn.classList.add("btn-primary");
                }, 3000);
              }
            })
            .catch(function (error) {
              console.error("❌ Erreur réseau:", error);

              submitBtn.textContent = "Erreur";
              submitBtn.classList.remove("btn-primary");
              submitBtn.classList.add("btn-danger");

              alert("❌ Erreur de connexion");

              // Réinitialiser le bouton
              setTimeout(function () {
                submitBtn.textContent = originalText;
                submitBtn.disabled = false;
                submitBtn.classList.remove("btn-danger");
                submitBtn.classList.add("btn-primary");
              }, 3000);
            });
        });
      } else {
        console.error("❌ Modal ou formulaire non trouvé");
      }

      // Fonction pour afficher des notifications (simplifiée)
      function showNotification(message, type) {
        console.log("📢 Notification:", message, type);
        alert(message);
      }

      // Initialiser la date minimale pour l'objectif d'épargne
      const deadlineField = document.getElementById("deadline");
      if (deadlineField) {
        const today = new Date();
        const formattedDate = today.toISOString().split("T")[0];
        deadlineField.min = formattedDate;
        deadlineField.value = formattedDate;
      }

      // Validation du formulaire d'objectif d'épargne
      const savingsForm = document.getElementById("savingsGoalForm");
      if (savingsForm) {
        savingsForm.addEventListener("submit", function (e) {
          let isValid = true;
          const title = document.getElementById("title").value.trim();
          const targetAmount = parseFloat(
            document.getElementById("targetAmount").value
          );
          const deadline = document.getElementById("deadline").value;

          if (title.length < 2 || title.length > 200) {
            isValid = false;
            alert("Le titre doit comporter entre 2 et 200 caractères");
          }

          if (isNaN(targetAmount) || targetAmount <= 0) {
            isValid = false;
            alert("Le montant cible doit être un nombre positif supérieur à 0");
          }

          if (!deadline) {
            isValid = false;
            alert("Veuillez choisir une date limite");
          }

          if (!isValid) {
            e.preventDefault();
          }
        });
      }
    </script>

    <!-- Modal Objectif d'Épargne -->
    <div
      class="modal fade"
      id="createSavingsGoalModal"
      tabindex="-1"
      aria-labelledby="createSavingsGoalModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-lg">
        <div class="modal-content">
          <div class="modal-header bg-success text-white">
            <h5 class="modal-title" id="createSavingsGoalModalLabel">
              <i class="fas fa-piggy-bank me-2"></i>
              Nouvel objectif d'épargne
            </h5>
            <button
              type="button"
              class="btn-close btn-close-white"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <form action="/savings" method="POST" id="savingsGoalForm">
              <div class="mb-3">
                <label for="title" class="form-label"
                  >Titre de l'objectif</label
                >
                <input
                  type="text"
                  class="form-control"
                  id="title"
                  name="title"
                  required
                  placeholder="Ex: Vacances d'été, Nouvel ordinateur..."
                  minlength="2"
                  maxlength="200"
                />
              </div>

              <div class="mb-3">
                <label for="targetAmount" class="form-label"
                  >Montant cible (€)</label
                >
                <div class="input-group">
                  <span class="input-group-text"
                    ><i class="fas fa-euro-sign"></i
                  ></span>
                  <input
                    type="number"
                    class="form-control"
                    id="targetAmount"
                    name="targetAmount"
                    required
                    step="0.01"
                    min="0.01"
                    placeholder="Montant à atteindre"
                  />
                </div>
              </div>

              <div class="mb-3">
                <label for="deadline" class="form-label">Date limite</label>
                <input
                  type="date"
                  class="form-control"
                  id="deadline"
                  name="deadline"
                  required
                />
              </div>

              <div class="d-grid gap-2">
                <button type="submit" class="btn btn-success">
                  <i class="fas fa-save me-2"></i>
                  Créer l'objectif d'épargne
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
